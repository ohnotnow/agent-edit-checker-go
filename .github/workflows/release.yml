name: Release

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Build binaries
        run: |
          # macOS
          GOOS=darwin GOARCH=amd64  go build -ldflags="-s -w" -o dist/aec-darwin-amd64 ./cmd/aec
          GOOS=darwin GOARCH=arm64  go build -ldflags="-s -w" -o dist/aec-darwin-arm64 ./cmd/aec

          # Linux
          GOOS=linux GOARCH=amd64   go build -ldflags="-s -w" -o dist/aec-linux-amd64 ./cmd/aec
          GOOS=linux GOARCH=arm64   go build -ldflags="-s -w" -o dist/aec-linux-arm64 ./cmd/aec

          # Windows
          GOOS=windows GOARCH=amd64 go build -ldflags="-s -w" -o dist/aec-windows-amd64.exe ./cmd/aec

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: dist/*
